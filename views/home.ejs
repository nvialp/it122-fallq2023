<html>
    <body>
        <h1>
            My Home Page
        </h1>

        <% rovers.forEach(function(rovers) { %>
            <li> <a href = "/rover/<%= rovers.name%>"><%= rovers.name %> </a></li>
        <% }); %>
    </body>
</html>