<html>
    <body>
        <h1>
            My Home Page
        </h1>

        <% showcase.forEach(function(showcase) { %>
            <li> <%= rovers.name %> </li>
        <% }); %>
    </body>
</html>